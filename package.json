{
  "name": "pushbox-hyrule",
  "version": "0.1.0",
  "private": true,
  "description": "PushBox HTML5 port plus the Hyrule isometric MMORPG prototype",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "test": "node tools/headless_test.js && node tools/zelda_test.js",
    "check": "node --check js/*.js && node --check zelda/js/*.js && node --check server/*.js && node --check shared/*.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.2",
    "@fastify/static": "^8.1.1",
    "@fastify/websocket": "^11.0.2",
    "fastify": "^5.2.1"
  }
}
